# Phase 3a モデル選定の評価条件（案）

**状態：案（2026-09-24、版2）。ユーザーの承認前であり、この条件による比較実験はまだ実行しない。** 承認した時点のcommit IDを「固定版」とし、以後の結果にはその固定版を記録する。数値は特に断らない限りすべて仮の値である。

根拠は[開発計画](../plans/development-plan.md)のPhase 3a、[変換エンジンの調査](research/conversion-engines.md)の「単一ニューラルモデルを決める実験」、[評価計画](research/ux-and-evaluation.md)、[ニューラル直接変換の設計案](neural-direct-conversion-design.md)、[ローカル試作](neural-probe.md)、[Phase 2cの評価条件](phase2c-evaluation-protocol.md)である。

## 目的と仮説

目的は、「ニューラルで直接変換し、辞書で補う方式」で製品に載せるモデルを一つ選ぶことである。製品にはモデルの切り替えを設けず、選んだ一つだけを配布する。

| 番号 | 仮説 | 反証となる結果 |
| --- | --- | --- |
| H1 | 候補のモデルを使うと、Mozcだけのライブ変換より、無介入正解文率が上がる | 補正した区間の下限が0以下 |
| H2 | 候補のモデルを使っても、訂正操作と表示の揺れは悪化しない | 訂正操作数の平均差またはflickerの比が、補正した区間の上限で許容幅を超える |
| H3 | 候補のモデルを使っても、過去の正しい表記、stable・chosen、入力した数字・英字を書き換えず、かなの読みから辞書の候補に無い数字・英字を作って表示しない | 適用後の違反が1件でもある |
| H4 | 候補のモデルは、端末で速度・メモリ・電池の目標に収まる | どれか一つでも目標を超える |

H3は件数の多少ではなく発生の有無で判断する。入力した数字・英字はモデルへ渡さない設計のため、その改変はニューラルの条件では原則0になる。ニューラルに固有の失敗（かなの読みから誤った数字を作る。例：「じゅうじ」→「11時」「0時」、「にせんにじゅうろくねん」→「1999年」「26年」）は、下の「H3の数え方」で検査とは独立に数える。

## 比較条件

| 条件 | 内容 |
| --- | --- |
| M | Mozcだけのライブ変換（ニューラルを無効にした同じAPK）。基準線 |
| ZS | zenz v3.2 small Q5_K_M |
| ZX | zenz v3.2 xsmall Q5_K_M |
| JS | jinen v2 small Q5_K_M |
| JX | jinen v2 xsmall Q5_K_M |

- モデルのrevisionとSHA-256は[ローカル試作](neural-probe.md#モデルhugging-facerevision固定)の値に固定する。
- runtimeは4モデルとも同じllama.cpp（azooKey fork `66afb885a8ddcc0511798aec9fb5a01017941018`）にする。スミレと同じ`88b97a47`はjinen v2（`qwen3`）を読めないためである。
- 量子化は4モデルともQ5_K_Mにそろえる。量子化による品質低下を切り分ける追加の比較（jinen v2 smallのQ8_0とF16）は、Q5_K_Mの結果を見た後に別途承認を得て行う（下の規則では「探索」に当たる）。zenz v3.2はQ5_K_M以外の公開を確認していないため、同じ切り分けはできない。
- 全条件で同じ`NeuralRangeConverter`（同じcommit）と同じMozc（commit、`mozc.data`）を使い、モデルとモデルごとのプロンプト形式だけを変える。プロンプト形式は段階0で確かめてから固定する。
- 推論の設定は全モデルで同じにする：threads 4、`n_ctx` 512、greedy、最大出力token 64、時間超過300 ms。段階0の結果で最大出力tokenだけは変えてよい（下記）。
- ユーザー辞書は空、学習履歴は各条件の開始前に消す。

## 課題集合

| 集合 | 中身 | 用途 | 利用条件 |
| --- | --- | --- | --- |
| 開発用 | [`phase3a-dev-tasks.tsv`](phase3a-dev-tasks.tsv)の30文（9分類、架空の内容） | 段階0の入力形式の確認、設定の調整、手順の確認 | 本書と同じcommitで作成済み。本書の承認と同時に文と許容表記を確定する。Phase 2cの54文と読み・許容表記が一致する文は無い（照合済み） |
| 試験用A | [`phase2c-tasks.tsv`](phase2c-tasks.tsv)の評価用54文 | 逐次入力の再生（主指標） | 承認済みの課題と許容表記をそのまま使い、追加・削除しない |
| 試験用B | [AJIMEE-Bench](https://github.com/azooKey/AJIMEE-Bench/tree/401666cd56d1a570c2021798b64b6da4396bfd45) `JWTD_v2/v1`の200問（commit `401666cd56d1a570c2021798b64b6da4396bfd45`） | 一回の変換の正解率（補助指標） | 下記 |

- 開発用の文は、Phase 2cの課題と同じ列（`id`、`分類`、`リスト`、`読み`、`許容表記`、`指示`）で書く。数値の分類には、かなで入力した読みから数字が生まれる文（`VD01`、`VD03`）を入れ、検査4とH3の数え方を開発用の段階で動かせるようにした。
- 試験用Aは、Phase 2cの常用試験の課題でもある。Phase 3aで結果を見た後に設定を変えると、以後この54文は調整に使った集合になる。
- 試験用Bのデータは CC BY-SA 3.0（元の日本語Wikipedia入力誤りデータセットv2に準拠）、評価コード`utils.py`と`test_utils.py`はCC0 1.0とREADMEに書かれている。データはGit追跡外の`.local-build/phase3a/`へ取得し、リポジトリには入れない。リポジトリへは問題の`index`ごとの正誤と集計値だけを残す。モデルの出力文字列を問題ごとに公開するかは、派生物としての扱いを確認してから決める（要確認）。
- 試験用Bは入力が完成したカタカナの読みで、左文脈付き100問と文脈なし100問からなる。逐次入力やライブ変換の揺れは測れない。また、jinenの作者はこのベンチマークで自作モデルの値を公表しており、モデルの開発でこのベンチマークを見た可能性を否定できない。元データはWikipedia由来で、各モデルの学習データに同じ文が含まれるかも不明である。このため**補助指標にとどめ、この集合だけの差でモデルを選ばない**。

## 手順の順序

1. 本書と開発用の30文を承認する。
2. 段階0（入力形式の確認）と、開発用の集合での設定の調整を行う。
3. 調整を終えた設定（プロンプト形式、最大出力token、時間超過、長い入力の区切り、検査の規則、評価手順）と、下の「固定と変更の規則」の各commit・SHA-256を記録して**最終設定として固定する**。固定した内容は本書の追記として残す。
4. 試験用A・Bを、固定した最終設定で各条件に流す（段階1・段階2）。
5. 選び方に従って候補を決め、配布可否の関門を通す。

## 段階0：入力形式の確認（開発用の集合だけ）

モデルごとのプロンプト形式と停止条件を、試験の前に開発用の集合で確かめる。結果を記録してから形式を固定する。

- **tokenizerの一致（jinen）**：作者の`tokenizer.json`（jinen v2 small・xsmallの同じrevisionにあるもの、2,047,498 bytes。取得は承認事項）とllama.cppの内蔵tokenizerで、開発用30文の全プロンプト（段階2の逐次入力で実際に作られる各プロンプト。左文脈を含む）のtoken列を比べる。1件でも一致しなければ形式を固定せず、不一致の例を示して対応（内蔵tokenizerの修正、`tokenizer.json`を使う実装、該当文字の前処理）を決め直す。zenzは作者のtokenizerの別ファイルが無いため、内蔵tokenizerを基準にする。
- **前処理**：次の形式を固定の候補とし、開発用の集合で出力が空・途中で終わる・区切り記号を含む割合を記録する。
  - zenz：読みをカタカナへ写す。左文脈があるときだけ`U+EE02`を付ける。半角空白を全角へ、改行を除く。
  - jinen：読みをカタカナへ写す。左文脈が空でも`U+EE02`を付ける。プロンプト全体をNFKC正規化する。
- **停止条件**：EOGか、出力中の`U+EE00..U+EE0F`で止める。最大出力tokenに達した場合（打ち切り）は検査1・2で捨てる。モデルごとに、開発用の集合での打ち切り率（打ち切った要求数÷要求数）を記録する。打ち切り率が1%を超えるモデルは、最大出力tokenを96に上げて一度だけ測り直し、結果を記録する。それでも1%を超えるモデルは、打ち切り率を報告した上で試験へ進め、段階2の時間超過率・不採用率で評価する。
- **文字数の上限**：tokenの数え方がモデルで違う（zenzはほぼ1文字1 token、`U+EE00`などは3 token。jinenは語の単位で、区切り記号は1 token）ため、上限はtoken数ではなく文字数で全モデル共通にする。モデルへ渡すかなの連なりは最大30文字、左文脈は最大64文字とする。開発用の集合で、各モデルの最大のプロンプトtoken数＋最大出力tokenが`n_ctx`（512）に収まることを確かめて記録する。収まらないモデルがあれば、全モデル共通の文字数の上限を下げる（モデルごとに変えない）。

## 測り方

### 段階1：Macでの一回の変換（モデルだけ、補助）

- 試験用Bの200問と、試験用Aの54文の`読み`全体を、各モデルで一回ずつ変換する。Mozcは使わない（Mac向けのMozcをビルドしていないため）。検査4は辞書が要るため、ここでは適用しない。
- 読みの形式：試験用Bはデータのカタカナのまま、試験用Aはひらがなをカタカナへ写す。左文脈は試験用Bの`context_text`だけを使う。
- 正解：試験用Bは`expected_output`のいずれかとの完全一致（exact@1）。NFKCをかけた一致は参考として併記する。試験用Aは許容表記のいずれかとの完全一致。
- 数字の照合：下の「最終文の数字の並び」の規則を、出力と正解の組にも当てはめ、件数を記録する。
- 推論時間の分布も参考として記録する。端末の性能の根拠にはしない。

### 段階2：端末での逐次入力の再生（主指標）

- Phase 2cの自動測定の手順（`adb`によるキー入力、評価用計数、`uiautomator`による最終文の取得）で、試験用Aの54文を流す。人の操作は含めない。
- **同一のAPKと計数**：Mを含む全条件を、同じAPK（同じcommit、同じSHA-256）、同じ評価用計数の版で測る。条件の切り替えはdebug buildの設定（`adb`から送る評価用の設定）で、ニューラルの無効化（M）とモデルのファイルの指定（ZS・ZX・JS・JX）だけを変える。Mでもニューラル側の計数の列は出し、0であることを確かめる。
- **同一の入力間隔**：キーの間隔は全条件で150 ms（Phase 2cの値）に固定する。
- **回と順番**：品質・速度・メモリは5回に分けて測る。各回で5条件を一つずつ流し、条件の順番は5×5のラテン方格で入れ替える（各条件が各位置に一度ずつ当たる）。方格の行の割り当てはseed `20261015`で決め、記録する。各条件の間に5分以上空け、`dumpsys thermalservice`の温度状態が`NONE`に戻ってから始める。
- 品質の指標は、文ごとに5回の値の平均をとり、その文を一単位として集計する（greedyのため回ごとの差は主に時間超過と古い結果の破棄から生じる。回ごとの値も併記する）。
- **ニューラル側の計数**（本文は残さず件数と時間だけ）：モデルへの要求数、かなの連なりへの分割の数（部分範囲あたり）、長い入力を区切った数、cache hitの数、中断の数、時間超過の数、検査で捨てた数（検査1〜4の別）、Mozcへ切り替えた数、読みのまま表示した数、推論時間、入力の受理から結果の適用までの時間、下の「H3の数え方」の各件数。これらの計数の実装とJVMテストは、試験を始める前に済ませる（本書の範囲外）。結果の表には、品質の値と並べて分割とfallbackの回数を必ず示す。

### H3の数え方（検査とは独立に数える）

検査4が正しく働いていることを前提にせず、次の3つを分けて数える。

| 計数 | 数える時点 | 定義 |
| --- | --- | --- |
| 生成件数 | 検査の前 | モデルの出力に含まれる数字・英字の連なりのうち、同じ読みの範囲をちょうど覆うMozcの文節の候補の連結と完全一致しないものの数。検査の実装とは別の関数で数える |
| 拒否件数 | 検査の結果 | 検査4がその出力を捨てた数 |
| 適用後の違反件数 | コアへ適用した後 | 表示へ適用した結果のsegmentのうち、生成件数の定義に当たる数字・英字を含むものの数。検査が正しければ0 |

さらに、**最終文の数字の並び**を許容表記と独立に照合する。最終文と各許容表記から、数字の連なり（全角は半角へ直す）を順に取り出した列を作り、最終文の列がどの許容表記の列とも一致しない文を「数字の違反」として数える。許容表記に漢数字の表記（例：「三百円」）と算用数字の表記（「300円」）の両方がある場合は、どちらかの列と一致すればよい。この照合は最終文が許容表記と完全一致しない文にも行う。

H3の判定は、適用後の違反件数と数字の違反が全条件で0件であることとする。生成件数と拒否件数は、候補ごとの傾向として報告する（安全基準の判定には使わない）。ただし、生成件数に当たる出力のうち検査1〜4のどれでも捨てられなかったものが1件でもあれば、適用後の違反件数が0でも、検査か計数の不具合として試験を止める。

### 指標

| 指標 | 定義 | 段階 |
| --- | --- | --- |
| exact@1 | 一回の変換の出力が正解のいずれかと完全に一致した割合 | 1 |
| 無介入正解文率 | 読みの入力と本来の終端操作だけで、最終文が許容表記に一致した文の割合（Phase 2cと同じ定義） | 2 |
| 訂正操作数の下限 | 最終文が許容表記と一致しない場合に、候補の照合で機械的に直すのに必要な操作数（Phase 2cと同じ手順）。文ごとの値と、条件ごとの総数を示す | 2 |
| 最終正解率 | 機械的な訂正の後に許容表記へ到達できた文の割合。候補に正解が無い文を分けて数える | 2 |
| flicker | 変換結果の到着で、既に変換済みの表示が変わった回数を読み100文字あたりで表す（Phase 2cの評価用計数と同じ） | 2 |
| 誤書換え | `stable_overwrites`、`chosen_overwrites`、入力した数字・英字の改変の数、H3の適用後の違反件数、数字の違反 | 2 |
| 不採用率 | モデルの出力のうち、検査1〜4で捨てた割合（検査ごとに分ける） | 2 |
| 時間超過率 | 推論が時間超過でMozcへ切り替わった割合 | 2 |
| 推論時間 | 下の「速度の母集団」参照。p50、p90、p95、p99 | 2 |
| 適用までの時間 | 下の「速度の母集団」参照。p50、p90、p95、p99 | 2 |
| 常駐メモリ | `:neural`とIMEのプロセスのPSSとその合計。モデル読み込み前、読み込み後の待機、連続入力中の最大。`dumpsys meminfo`で1秒ごと | 2 |
| cold start | `:neural`が無い状態からbindし、最初の推論結果が返るまで。10回の中央値と最大 | 2 |
| メモリ制限による終了 | `am memory-limiter status`と`ApplicationExitInfo`（`REASON_OTHER`かつ`MemoryLimiter:AnonSwap`）の記録 | 2 |
| 電池 | 下の「電池の測り方」参照 | 2 |

### 速度の母集団

- **推論時間**：5回の再生で、モデルへ実際に送った要求（cache hitを除く）すべて。時間超過した要求は、時間超過の値（300 ms）で打ち切った値として含める（p95が300 msに張り付けば、それ自体が不合格を示す）。新しい入力で中断した要求は含めず、件数と中断までの時間を別に示す。cache hitの数と割合を併記する。
- **適用までの時間**：5回の再生で、コアへ結果を返したすべての変換要求（cache hit、Mozcへの切り替え、読みのままの表示を含む）について、その要求を作った入力の受理から結果の適用までの時間。コアが古い結果として捨てた要求は含めず、件数を別に示す。
- p95などは条件ごとに全要求を一つにまとめて計算する。回ごとのp95も併記し、回によって合否が分かれる場合はその旨を記録する。

### 電池の測り方

- 試験用Aの54文の再生を10分間くり返し、充電を切った状態（`dumpsys battery unplug`）の`dumpsys batterystats`から端末全体の推定消費を得る。試験後に`dumpsys battery reset`で戻す。
- 各条件を3回測る。3回を3つの別の日または時間帯に分け、各回で5条件の順番をラテン方格の別の行で入れ替える。
- 同じ回の中のMとの比（候補÷M）をとり、3回の比の平均で判定する。3回の比の最小値と最大値が基準（1.2）をまたぐ場合は判定不能とし、2回を追加して5回の平均で判定する。追加後もまたぐ場合は、安全側として不合格とする。

## 実機の条件

- 端末はPixel 10 Pro（Android 17）。`ro.build.fingerprint`、画面の明るさ（固定値）、リフレッシュレート、省電力の設定、充電の状態、温度状態、`am memory-limiter status`を記録する。
- USB給電の影響が残るため、電池の値は条件間の比としてだけ使う。
- 別の担当が同じ端末で測定中の場合は、その測定が終わってから行う。
- Uzumiはdebuggableでないprofileableなbuild（release相当の設定）で測ることを基本とし、評価用計数と条件の切り替えを含めるため難しい場合はdebug buildとその理由を記録する。どちらの場合も全条件で同じAPKを使う。

## 合格基準（案、すべて仮の値）

以下の数値は仮であり、根拠となる測定はまだ無い。承認時に値を確定し、測定後に変えない。

| 対象 | 基準 |
| --- | --- |
| 安全（必須） | 誤書換え（stable、chosen、入力した数字・英字の改変、H3の適用後の違反件数、数字の違反）、crash、ANR、メモリ制限による終了が0件 |
| 速度 | 推論時間のp95 ≤ 100 ms、適用までの時間のp95 ≤ 300 ms、時間超過率 ≤ 1% |
| メモリ | 連続入力中の最大PSSが、`:neural`でsmall ≤ 150 MB・xsmall ≤ 80 MB、IMEと`:neural`の合計でMの条件のIMEの最大PSS＋150 MB（small）・＋80 MB（xsmall）以下 |
| cold start | 中央値 ≤ 2 s。IMEの入力はモデルの準備を待たずにできる |
| 電池 | 上の「電池の測り方」による比 ≤ 1.2 |
| 品質（H1） | 無介入正解文率の、候補とMの文ごとの差の平均について、補正した区間の下限 > 0 |
| 品質（H2） | 訂正操作数の下限の、候補とMの文ごとの差の平均について、補正した区間の上限 ≤ +0.1（1文あたり）。flickerの比（候補÷M）の、補正した区間の上限 ≤ 1.5 |

### 多重比較と区間の求め方

- 4候補をそれぞれMと比べるため、H1・H2の区間は同時に4回判定する。Bonferroniの補正で、各比較の区間を両側98.75%（1−0.05/4）とする。
- 区間は文を単位とする対応のあるbootstrap（10,000回、seed `20261015`）で求める。flickerの比は、再標本ごとに読みの文字数で重みづけた比を計算する。
- 検定の有意差は合否の条件にしない。区間の幅と、文ごとの差の分布の図も示す。
- 試験用Aは54文と少ないため、差があっても区間が0をまたぎ、「差が決まらない」結果になりやすい。その場合は下の選び方に従い、文の数を後から増やして同じ集合で判定し直すことはしない（新しい集合が必要なら下の規則に従う）。
- 分類ごとの結果を表と図で示す。全体が合格でも、特定の分類（数値、URL、英語混在など）で安全の不具合があればその候補は不合格とする。

## 選び方

1. 安全、速度、メモリ、cold start、電池の基準をすべて満たす候補だけを残す。
2. 残った候補のうち、H1・H2を満たすものを残す。
3. その中で、無介入正解文率の平均が最も高い候補を「最良」とする。最良との文ごとの差の平均について、対応のあるbootstrapの区間（候補間の比較は残った候補の数をkとして、k−1回の比較でBonferroni補正）が0を含む候補を「差が決まらない候補」とする。
4. 最良と差が決まらない候補の中から、次の順で一つに決める：連続入力中の`:neural`の最大PSSが小さい方、電池の比が小さい方、モデルのファイルが小さい方。
5. 試験用Bのexact@1は、4でも決まらない場合（値が同じ）の参考にだけ使う。
6. 残る候補が無ければ、Mozcだけのライブ変換を続ける。Q8_0などの量子化、モデルの追加、方式の見直しのどれに進むかは、結果を示して改めて判断する。
7. 選んだ候補を、次の「配布可否の関門」に通す。

## 配布可否の関門

製品への採用を確定する前に、選んだ候補のモデルと関係するデータの配布条件を確かめる。一つでも未解決なら、判定は「**採用保留**」とし、APKへ同梱しない。選定の結果（どの候補が基準を満たしたか）は記録として残す。

| 対象 | 確かめること |
| --- | --- |
| jinen v2（CC BY-SA 4.0） | 帰属表示の文言と表示場所、改変（量子化・変換）した重みを配る場合のShareAlikeの範囲、アプリ本体のライセンスとの関係、model cardが求める国立国会図書館の書誌データの出典表示 |
| zenz v3.2（Hubの表示はApache-2.0） | model card本文にライセンス以外の説明が無いこと、学習データの由来と条件、NOTICEに載せる文言 |
| llama.cpp（MIT） | 著作権表示の添付、azooKey forkの変更部分の条件 |
| 同梱するtokenizerのファイル（使う場合） | モデルと同じ条件か |
| AJIMEE-Bench | 配布物には含めない。評価結果の公開範囲だけを確かめる |

## 固定と変更の規則

- 承認後は、比較条件、モデルのrevisionとSHA-256、runtimeのcommit、課題集合、指標の定義、H3の数え方、合格基準、多重比較の方法、選び方、関門、seedを変えない。
- 段階0と開発用の集合での調整が終わった時点で、プロンプト形式、最大出力token、時間超過、文字数の上限、検査の規則、評価手順を最終設定として固定する。あわせて次のcommitとSHA-256を記録する：Uzumiのcommit、APKのSHA-256、`NeuralRangeConverter`を含むcommit、Mozcのcommitと`getDataVersion()`、llama.cppのcommit、`.so`のSHA-256、モデルのSHA-256、評価用計数の版。
- 試験の途中で不具合を直した場合は、直す前の結果を消さず、直した後に**全条件を**同じ版で流し直す。一部の条件だけを流し直して比べない。

## test集合を調整に使わない規則

- 最終設定に含まれる項目の調整は、開発用の集合だけで行う。
- 試験用の文を、プロンプトの例、辞書、学習、テストのfixtureへ入れない。
- **試験用A・Bの結果を見た後の改版は「探索」とする。** 探索の結果は探索と明記して報告し、採用の判断には使わない。探索で有望な版が見つかった場合、採用の判断には、試験用A・B・開発用のどれとも重ならない**新しい未使用の集合**を用意し、同じ手順（作成、承認、最終設定の固定、一度だけの試験）で判定する。新しい集合は作成の時点で承認を得る。
- 試験用Aを探索に使った場合は、Phase 2cの常用試験の結果を読むときにその旨を併記する。
- 開発用の集合で見つかった誤りの修正は許す。修正の内容と、どの開発用の文で見つかったかを記録する。

## データの保存

- 生の記録（モデルの出力、評価用計数、`dumpsys`の出力、AJIMEE-Benchのデータ）はGit追跡外の`.local-build/phase3a/<実施日>/`に置く。
- リポジトリへは、課題IDごとの正誤と数値、集計表、図、図の元データ、生成コードを`docs/`へ残す。
- 日常入力の本文は使わない。記録に本文、候補、clipboardを残さない（試験用・開発用の文とその出力を除く）。

## 承認を求める事項

1. 比較条件：Mozcだけを基準線にし、4モデルをQ5_K_Mだけで比べること。量子化の切り分けは後で別に判断すること。runtimeを4モデルとも`66afb885`にそろえること。
2. 開発用の30文（[`phase3a-dev-tasks.tsv`](phase3a-dev-tasks.tsv)）と許容表記を、本書と同時に確定すること。
3. 段階0：jinenの`tokenizer.json`（2ファイル、各2,047,498 bytes）を取得してtokenizerの一致を確かめること。前処理の候補、打ち切り率1%の基準と最大出力tokenを96へ一度だけ上げる規則、全モデル共通の文字数の上限（かな30文字、左文脈64文字）。
4. 試験用A（Phase 2cの54文）を主指標、試験用B（AJIMEE-Bench）を補助指標とし、結果を見た後の改版は探索として扱い、採用の判断には新しい未使用の集合を用意すること。
5. H3を検査と独立に数えること（生成・拒否・適用後の違反の3つと、最終文の数字の並びの照合）。
6. 段階2を、Mを含む全条件で同じAPK・同じ計数・入力間隔150 msで、5回×ラテン方格の順番で測ること。
7. 速度の母集団（時間超過は300 msで打ち切って含める、中断は除いて別に示す、cache hitは推論時間から除き適用までの時間には含める）と、IMEとの合計PSSの上限。
8. 電池を各条件3回（判定不能なら5回）測り、Mとの比の平均で判定し、なおまたぐ場合は不合格とすること。
9. 多重比較（Bonferroni、各98.75%）、H2の許容幅（訂正操作数の平均差+0.1、flickerの比1.5）、候補間で差が決まらない場合の規則（PSS、電池、ファイルの大きさの順）。
10. 配布可否の関門と、未解決なら「採用保留」とすること。
11. 追加する評価用計数の項目と、その実装を試験の前に行うこと。
